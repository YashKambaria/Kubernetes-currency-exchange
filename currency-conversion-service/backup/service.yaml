apiVersion: v1
kind: Service
metadata:
  name: currency-conversion
  namespace: default
spec:
  type: LoadBalancer
  selector:
    app: currency-conversion
  ports:
    - port: 8080
      targetPort: 8080
